//! Command-line entry point for the RTIC application generator.

#![deny(missing_docs)]

fn main() {
    eprintln!(
        "the V3 generator is not yet migrated; run `cargo check` or `cargo test` to validate definitions and composition"
    );
    std::process::exit(2);
}
